---
import PageLayout from '@/layouts/PageLayout.astro'
import { Image } from 'astro:assets'
import logo from '@/assets/logo.svg'
import config from '@/theme.config'

// You'll need to create or import these images
// These are placeholders - replace with actual image imports
const teamImage = '/joinus.png'

// Create proper frontmatter for PageLayout
const frontmatter = {
  title: 'About | Association of Software Engineering',
  description:
    'Learn about the Association of Software Engineering at NSBM, our history, mission, and key events',
  activeHeaderLink: 'About',
  searchable: true
}
---

<PageLayout {frontmatter}>
  <!-- Hero Section -->
  <div class="hero-container">
    <div class="logo-container fade-in">
      <Image
        src={logo}
        alt="ASE Logo"
        width={200}
        height={200}
        class="logo-pulse"
      />
    </div>

    <h1 class="title slide-up">
      <span class="text-accent">{'Association of'}</span>
      <br />
      <span class="text-neon-green">{'Software Engineering'}</span>
    </h1>

    <div class="subtitle slide-up-delay">
      <div class="typing-text">Innovate. Collaborate. Excel.</div>
    </div>

    <!-- Add the "Read More" button -->
    <button class="read-more-button" onclick="window.location.reload()">
      Read More
    </button>
  </div>

  <!-- About Section -->
  <section class="content-section fade-in-section">
    <h2 class="section-heading">About Us</h2>
    <div class="about-content">
      <div class="about-text">
        <p>
          The Association of Software Engineering at NSBM, formerly known as the
          Student Circle of Software Engineering, was established in February
          2024 under the guidance of the Department of Software Engineering and
          Information Systems. The association was formed to foster a
          collaborative environment where students following a discipline in
          Software Engineering could enhance their skills, share knowledge, and
          engage in various activities that complement their academic pursuits.
        </p>
      </div>
      <div class="about-image">
        <div class="floating-icons-grid">
          <span class="icon">{'<>'}</span>
          <span class="icon">⚙️</span>
          <span class="icon">🚀</span>
          <span class="icon">🗄️</span>
          <span class="icon">☁️</span>
          <span class="icon">🤖</span>
          <span class="icon">🖥️</span>
          <span class="icon">🐛</span>
          <span class="icon">🌐</span>
          <span class="icon">🔒</span>
          <span class="icon">💻</span>
          <span class="icon">📊</span>
          <span class="icon">🔗</span>
          <span class="icon">🛠️</span>
          <span class="icon">⛓️</span>
        </div>
      </div>
    </div>
  </section>

  <!-- Evolution Timeline -->
  <section class="timeline-section fade-in-section">
    <h2 class="section-heading">Evolution of the Association</h2>
    <div class="timeline">
      <div class="timeline-item">
        <div class="timeline-dot"></div>
        <div class="timeline-content slide-right">
          <h3>February 2024</h3>
          <p>
            Established as the Student Circle of Software Engineering under the
            Department of Software Engineering and Information Systems.
          </p>
        </div>
      </div>
      <div class="timeline-item">
        <div class="timeline-dot"></div>
        <div class="timeline-content slide-left">
          <h3>2024</h3>
          <p>
            Organized inaugural events including GreenEXE 2.0 and MadFest,
            establishing a foundation for future initiatives.
          </p>
        </div>
      </div>
      <div class="timeline-item">
        <div class="timeline-dot"></div>
        <div class="timeline-content slide-right">
          <h3>2025</h3>
          <p>
            New board election led to rebranding as the "Association of Software
            Engineering" to reflect growth and expanding scope.
          </p>
        </div>
      </div>
    </div>
  </section>

  <!-- Mission and Vision -->
  <section class="mission-section fade-in-section">
    <div class="mission-container">
      <h2 class="section-heading">Our Mission and Vision</h2>
      <div class="mission-cards">
        <div class="mission-card">
          <div class="card-icon mission-icon">
            <svg
              xmlns="http://www.w3.org/2000/svg"
              width="24"
              height="24"
              viewBox="0 0 24 24"
              fill="none"
              stroke="currentColor"
              stroke-width="2"
              stroke-linecap="round"
              stroke-linejoin="round"
              class="feather feather-target"
            >
              <circle cx="12" cy="12" r="10"></circle>
              <circle cx="12" cy="12" r="6"></circle>
              <circle cx="12" cy="12" r="2"></circle>
            </svg>
          </div>
          <h3>Mission</h3>
          <p>
            To empower software engineering students by providing opportunities
            for professional growth, networking, and skill development.
          </p>
        </div>
        <div class="mission-card">
          <div class="card-icon vision-icon">
            <svg
              xmlns="http://www.w3.org/2000/svg"
              width="24"
              height="24"
              viewBox="0 0 24 24"
              fill="none"
              stroke="currentColor"
              stroke-width="2"
              stroke-linecap="round"
              stroke-linejoin="round"
              class="feather feather-eye"
            >
              <path d="M1 12s4-8 11-8 11 8 11 8-4 8-11 8-11-8-11-8z"></path>
              <circle cx="12" cy="12" r="3"></circle>
            </svg>
          </div>
          <h3>Vision</h3>
          <p>
            To create a dynamic community where students can collaborate,
            innovate, and contribute meaningfully to the field of software
            engineering.
          </p>
        </div>
      </div>
    </div>
  </section>

  <section class="testimonials-section fade-in-section">
    <h2 class="section-heading">What Our Members Say</h2>

    <div class="testimonial-slider">
      <div class="testimonial-container">
        <!-- Testimonial 1 -->
        <div class="testimonial-card">
          <div class="quote-icon">
            <svg
              xmlns="http://www.w3.org/2000/svg"
              width="24"
              height="24"
              viewBox="0 0 24 24"
              fill="none"
              stroke="currentColor"
              stroke-width="2"
              stroke-linecap="round"
              stroke-linejoin="round"
              class="feather feather-message-circle"
            >
              <path
                d="M21 11.5a8.38 8.38 0 0 1-.9 3.8 8.5 8.5 0 0 1-7.6 4.7 8.38 8.38 0 0 1-3.8-.9L3 21l1.9-5.7a8.38 8.38 0 0 1-.9-3.8 8.5 8.5 0 0 1 4.7-7.6 8.38 8.38 0 0 1 3.8-.9h.5a8.48 8.48 0 0 1 8 8v.5z"
              ></path>
            </svg>
          </div>
          <p class="testimonial-text">
            "Being the Media Coordinator for the SCSE at NSBM has been an
            enriching experience. I've had the opportunity to craft engaging
            content, manage media outreach, and promote initiatives that bring
            our community together. This role has sharpened my skills in content
            creation, event management, and digital strategy while fostering a
            dynamic and collaborative environment. The circle is a hub for
            growth, innovation, and networking—perfect for anyone passionate
            about software engineering!"
          </p>
          <div class="testimonial-author">
            <div class="author-avatar">SF</div>
            <div class="author-info">
              <h4>Senesh Fitzroy</h4>
              <p>Former SCSE Media Coordinator</p>
            </div>
          </div>
        </div>

        <!-- Testimonial 2 -->
        <div class="testimonial-card">
          <div class="quote-icon">
            <svg
              xmlns="http://www.w3.org/2000/svg"
              width="24"
              height="24"
              viewBox="0 0 24 24"
              fill="none"
              stroke="currentColor"
              stroke-width="2"
              stroke-linecap="round"
              stroke-linejoin="round"
              class="feather feather-message-circle"
            >
              <path
                d="M21 11.5a8.38 8.38 0 0 1-.9 3.8 8.5 8.5 0 0 1-7.6 4.7 8.38 8.38 0 0 1-3.8-.9L3 21l1.9-5.7a8.38 8.38 0 0 1-.9-3.8 8.5 8.5 0 0 1 4.7-7.6 8.38 8.38 0 0 1 3.8-.9h.5a8.48 8.48 0 0 1 8 8v.5z"
              ></path>
            </svg>
          </div>
          <p class="testimonial-text">
            "Being part of this circle gave me incredible hands-on experience in
            group work and event planning. It was a great opportunity to have
            while progressing in my academic journey.✒️"
          </p>
          <div class="testimonial-author">
            <div class="author-avatar">RY</div>
            <div class="author-info">
              <h4>Ruwanthi Yashodara</h4>
              <p>Former Executive Member</p>
            </div>
          </div>
        </div>

        <!-- Testimonial 3 -->
        <div class="testimonial-card">
          <div class="quote-icon">
            <svg
              xmlns="http://www.w3.org/2000/svg"
              width="24"
              height="24"
              viewBox="0 0 24 24"
              fill="none"
              stroke="currentColor"
              stroke-width="2"
              stroke-linecap="round"
              stroke-linejoin="round"
              class="feather feather-message-circle"
            >
              <path
                d="M21 11.5a8.38 8.38 0 0 1-.9 3.8 8.5 8.5 0 0 1-7.6 4.7 8.38 8.38 0 0 1-3.8-.9L3 21l1.9-5.7a8.38 8.38 0 0 1-.9-3.8 8.5 8.5 0 0 1 4.7-7.6 8.38 8.38 0 0 1 3.8-.9h.5a8.48 8.48 0 0 1 8 8v.5z"
              ></path>
            </svg>
          </div>
          <p class="testimonial-text">
            "My time as the designer for the SCSE was an amazing journey of
            teamwork and creativity. Working alongside talented and supportive
            mates, I honed my design skills, tackled challenges, and grew both
            professionally and personally. Grateful for the friendships and
            experiences that made it truly special!"
          </p>
          <div class="testimonial-author">
            <div class="author-avatar">SP</div>
            <div class="author-info">
              <h4>Senuki Perera</h4>
              <p>Former SCSE Co-Designer</p>
            </div>
          </div>
        </div>

        <!-- Testimonial 4 -->
        <div class="testimonial-card">
          <div class="quote-icon">
            <svg
              xmlns="http://www.w3.org/2000/svg"
              width="24"
              height="24"
              viewBox="0 0 24 24"
              fill="none"
              stroke="currentColor"
              stroke-width="2"
              stroke-linecap="round"
              stroke-linejoin="round"
              class="feather feather-message-circle"
            >
              <path
                d="M21 11.5a8.38 8.38 0 0 1-.9 3.8 8.5 8.5 0 0 1-7.6 4.7 8.38 8.38 0 0 1-3.8-.9L3 21l1.9-5.7a8.38 8.38 0 0 1-.9-3.8 8.5 8.5 0 0 1 4.7-7.6 8.38 8.38 0 0 1 3.8-.9h.5a8.48 8.48 0 0 1 8 8v.5z"
              ></path>
            </svg>
          </div>
          <p class="testimonial-text">
            "Being a co-designer of the SCSE was an enriching experience. I
            gained teamwork, creativity, and leadership skills while working
            with amazing teammates who made the journey truly memorable."
          </p>
          <div class="testimonial-author">
            <div class="author-avatar">IW</div>
            <div class="author-info">
              <h4>Isira Wickramasinghe</h4>
              <p>Former SCSE Co-Designer</p>
            </div>
          </div>
        </div>

        <!-- Testimonial 5 -->
        <div class="testimonial-card">
          <div class="quote-icon">
            <svg
              xmlns="http://www.w3.org/2000/svg"
              width="24"
              height="24"
              viewBox="0 0 24 24"
              fill="none"
              stroke="currentColor"
              stroke-width="2"
              stroke-linecap="round"
              stroke-linejoin="round"
              class="feather feather-message-circle"
            >
              <path
                d="M21 11.5a8.38 8.38 0 0 1-.9 3.8 8.5 8.5 0 0 1-7.6 4.7 8.38 8.38 0 0 1-3.8-.9L3 21l1.9-5.7a8.38 8.38 0 0 1-.9-3.8 8.5 8.5 0 0 1 4.7-7.6 8.38 8.38 0 0 1 3.8-.9h.5a8.48 8.48 0 0 1 8 8v.5z"
              ></path>
            </svg>
          </div>
          <p class="testimonial-text">
            "Being part of the ASE helped me develop and build the leadership
            skills that complement my technical abilities big time. It's more
            than just a student association; it's a growth platform."
          </p>
          <div class="testimonial-author">
            <div class="author-avatar">DV</div>
            <div class="author-info">
              <h4>Darren Victoria</h4>
              <p>Former Vice President</p>
            </div>
          </div>
        </div>
      </div>

      <!-- Navigation Dots -->
      <div class="testimonial-nav">
        <button class="nav-dot active" data-index="0"></button>
        <button class="nav-dot" data-index="1"></button>
        <button class="nav-dot" data-index="2"></button>
        <button class="nav-dot" data-index="3"></button>
        <button class="nav-dot" data-index="4"></button>
      </div>
    </div>
  </section>

  <style>
    .card-icon {
      font-size: 3rem;
      margin-bottom: 1rem;
      color: #39ff14; /* Neon green color */
      display: flex;
      justify-content: center;
    }
  </style>

  <!-- Team Section -->
  <section class="team-section fade-in-section">
    <h2 class="section-heading">Join Our Community</h2>
    <div class="team-container">
      <div class="team-image-container">
        <img src={teamImage} alt="Team Working Together" class="team-image" />
      </div>
      <div class="team-text">
        <p>
          Be part of a thriving community of software engineers at NSBM. Whether
          you're interested in coding, design, project management, or simply
          want to connect with like-minded peers, the Association of Software
          Engineering welcomes you.
        </p>
        <div class="cta-button">
          <a href="https://chat.whatsapp.com/HVcDnZ380tpAooloxgLS8F"
            >Get Involved</a
          >
        </div>
      </div>
    </div>
  </section>
</PageLayout>

<style>
  /* Base Styles */
  section {
    margin-bottom: 6rem;
    opacity: 1; /* Changed from 0 to make sections initially visible */
    transform: translateY(0); /* Changed from 20px to fix initial position */
  }

  /* Fix heading visibility */
  .section-heading {
    font-size: 2.5rem;
    margin-bottom: 2rem;
    text-align: center;
    color: rgb(
      var(--accent)
    ); /* Direct color instead of gradient for better visibility */
    position: relative;
    font-weight: bold;
  }

  .section-heading::after {
    content: '';
    position: absolute;
    bottom: -10px;
    left: 50%;
    transform: translateX(-50%);
    width: 100px;
    height: 4px;
    background: linear-gradient(
      90deg,
      rgb(var(--accent)),
      rgb(var(--accent-light))
    );
    border-radius: 2px;
  }

  /* Hero section - updated to work with PageLayout */
  .hero-container {
    display: flex;
    flex-direction: column;
    align-items: center;
    justify-content: center;
    padding: 2rem 0 4rem;
    margin-bottom: 2rem;
  }

  .logo-container {
    margin-bottom: 2rem;
  }

  .logo-pulse {
    animation: pulse 2s infinite;
    width: 200px;
    height: 200px;
  }

  .title {
    font-size: 4rem;
    font-weight: 800;
    margin-bottom: 1.5rem;
    transform: translateY(30px);
    opacity: 0;
    animation: slideUp 0.8s forwards ease-out;
    animation-delay: 0.3s;
    line-height: 1.2;
    text-align: center;
  }

  .text-accent {
    color: rgb(49, 49, 160);
    text-shadow: 0 0 5px rgba(0, 0, 139, 0.5); /* Add text shadow for emphasis */
  }

  .text-neon-green {
    color: #39ff14; /* Neon green color */
    text-shadow: 0 0 5px #39ff14; /* Neon glow effect */
    font-weight: 900; /* Make bolder */
  }

  .subtitle {
    font-size: 1.5rem;
    font-weight: 400;
    color: #e0e0e0;
    transform: translateY(30px);
    opacity: 0;
    animation: slideUp 0.8s forwards ease-out;
    animation-delay: 0.6s;
  }

  /* Keep original h2 style for non-section headings if needed */
  h2:not(.section-heading) {
    font-size: 2.5rem;
    margin-bottom: 2rem;
    text-align: center;
    background: linear-gradient(
      90deg,
      rgb(var(--accent)),
      rgb(var(--accent-light))
    );
    -webkit-background-clip: text;
    background-clip: text;
    -webkit-text-fill-color: transparent;
    position: relative;
  }

  h2:not(.section-heading)::after {
    content: '';
    position: absolute;
    bottom: -10px;
    left: 50%;
    transform: translateX(-50%);
    width: 100px;
    height: 4px;
    background: linear-gradient(
      90deg,
      rgb(var(--accent)),
      rgb(var(--accent-light))
    );
    border-radius: 2px;
  }

  /* The rest of your existing styles */
  /* ...existing code... */

  /* Additional CSS to ensure heading visibility in dark mode */
  :global(.dark) .section-heading {
    color: rgb(var(--accent-light)); /* Brighter color for dark mode */
  }

  :global(.dark) .text-neon-green {
    text-shadow: 0 0 8px #39ff14; /* Stronger shadow in dark mode */
  }

  .fade-in-section {
    opacity: 0;
    transform: translateY(20px);
    transition:
      opacity 0.6s ease,
      transform 0.6s ease;
  }

  .typing-text {
    border-right: 3px solid rgb(var(--accent));
    white-space: nowrap;
    overflow: hidden;
    animation:
      typing 3.5s steps(30, end) 1s forwards,
      blink-caret 0.75s step-end infinite;
    width: 0;
  }

  /* About Section */
  /* About Section */
  .about-content {
    display: flex;
    gap: 2rem;
    align-items: center;
    flex-wrap: wrap;
  }

  .about-text {
    flex: 1;
    min-width: 300px;
    font-size: 1.1rem;
    line-height: 1.7;
    text-align: justify;
  }

  .about-image {
    flex: 1;
    min-width: 300px;
    display: flex;
    justify-content: center;
    position: relative;
  }

  .floating-icons-grid {
    display: grid;
    grid-template-columns: repeat(3, 1fr); /* 3 columns */
    gap: 1.5rem; /* Space between icons */
    width: 100%;
    max-width: 300px; /* Adjust based on your design */
  }

  .icon {
    font-size: 2.5rem;
    text-align: center;
    opacity: 0;
    transform: scale(0.8);
    animation: floatIn 0.8s ease-out forwards;
  }

  /* Add staggered delays for each icon */
  .icon:nth-child(1) {
    animation-delay: 0.1s;
  }
  .icon:nth-child(2) {
    animation-delay: 0.2s;
  }
  .icon:nth-child(3) {
    animation-delay: 0.3s;
  }
  .icon:nth-child(4) {
    animation-delay: 0.4s;
  }
  .icon:nth-child(5) {
    animation-delay: 0.5s;
  }
  .icon:nth-child(6) {
    animation-delay: 0.6s;
  }
  .icon:nth-child(7) {
    animation-delay: 0.7s;
  }
  .icon:nth-child(8) {
    animation-delay: 0.8s;
  }
  .icon:nth-child(9) {
    animation-delay: 0.9s;
  }
  .icon:nth-child(10) {
    animation-delay: 1s;
  }
  .icon:nth-child(11) {
    animation-delay: 1.1s;
  }
  .icon:nth-child(12) {
    animation-delay: 1.2s;
  }
  .icon:nth-child(13) {
    animation-delay: 1.3s;
  }
  .icon:nth-child(14) {
    animation-delay: 1.4s;
  }
  .icon:nth-child(15) {
    animation-delay: 1.5s;
  }

  /* Float-in animation */
  @keyframes floatIn {
    0% {
      opacity: 0;
      transform: scale(0.8) translateY(20px);
    }
    100% {
      opacity: 1;
      transform: scale(1) translateY(0);
    }
  }

  /* Hover effect for icons */
  .icon:hover {
    transform: scale(1.1);
    transition: transform 0.3s ease;
  }

  /* Timeline Section */
  .timeline {
    position: relative;
    max-width: 900px;
    margin: 0 auto;
    padding: 2rem 0;
  }

  .timeline::before {
    content: '';
    position: absolute;
    width: 4px;
    background: linear-gradient(
      to bottom,
      rgba(var(--accent), 0.2),
      rgba(var(--accent), 0.8)
    );
    top: 0;
    bottom: 0;
    left: 50%;
    margin-left: -2px;
    border-radius: 2px;
  }

  .timeline-item {
    padding: 10px 40px;
    position: relative;
    width: 50%;
    box-sizing: border-box;
    margin: 2rem 0;
  }

  .timeline-item:nth-child(odd) {
    left: 0;
    text-align: right;
  }

  .timeline-item:nth-child(even) {
    left: 50%;
    text-align: left;
  }

  .timeline-dot {
    width: 20px;
    height: 20px;
    background-color: rgb(var(--accent));
    border-radius: 50%;
    position: absolute;
    top: 20px;
    right: -10px;
    z-index: 10;
    box-shadow: 0 0 10px rgba(var(--accent), 0.5);
  }

  .timeline-item:nth-child(even) .timeline-dot {
    left: -10px;
    visibility: hidden;
  }

  .timeline-content {
    padding: 20px;
    background: rgba(255, 255, 255, 0.1);
    backdrop-filter: blur(10px);
    border: 1px solid rgba(var(--accent), 0.2);
    border-radius: 10px;
    box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
    opacity: 0;
    transform: translateX(50px);
    transition: all 0.6s ease;
  }

  .timeline-item:nth-child(even) .timeline-content {
    transform: translateX(-25px);
  }

  .timeline-content h3 {
    color: rgb(var(--accent));
    margin-top: 0;
  }

  /* Mission and Vision Section */
  .mission-container {
    text-align: center;
  }

  .mission-cards {
    display: flex;
    justify-content: center;
    gap: 2rem;
    flex-wrap: wrap;
  }

  .mission-card {
    flex: 1;
    min-width: 250px;
    max-width: 350px;
    padding: 2rem;
    border-radius: 10px;
    background: rgba(255, 255, 255, 0.05);
    backdrop-filter: blur(10px);
    border: 1px solid rgba(var(--accent), 0.2);
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
    transition: transform 0.3s ease;
  }

  .mission-card:hover {
    transform: translateY(-10px);
  }

  .card-icon {
    font-size: 3rem;
    margin-bottom: 1rem;
    color: #39ff14; /* Neon green color */
  }

  .card-icon svg {
    width: 48px;
    height: 48px;
    stroke: #39ff14; /* Neon green color */
  }

  /* Team Section */
  .team-container {
    display: flex;
    align-items: center;
    gap: 2rem;
    flex-wrap: wrap;
  }

  .team-image-container {
    flex: 1;
    min-width: 300px;
    overflow: hidden;
    border-radius: 10px;
  }

  .team-image {
    width: 100%;
    height: auto;
    transition: transform 0.5s ease;
  }

  .team-image:hover {
    transform: scale(1.05);
  }

  .team-text {
    flex: 1;
    min-width: 300px;
    font-size: 1.1rem;
    line-height: 1.7;
  }

  .cta-button {
    margin-top: 2rem;
  }

  .cta-button a {
    display: inline-block;
    padding: 12px 24px;
    background: linear-gradient(
      90deg,
      rgb(var(--accent)),
      rgb(var(--accent-light))
    );
    color: white;
    text-decoration: none;
    border-radius: 30px;
    font-weight: 600;
    transition:
      transform 0.3s ease,
      box-shadow 0.3s ease;
    box-shadow: 0 5px 15px rgba(var(--accent), 0.4);
  }

  .cta-button a:hover {
    transform: translateY(-3px);
    box-shadow: 0 8px 20px rgba(var(--accent), 0.6);
  }

  /* Animations */
  @keyframes pulse {
    0% {
      transform: scale(1);
    }
    50% {
      transform: scale(1.05);
    }
    100% {
      transform: scale(1);
    }
  }

  @keyframes slideUp {
    to {
      transform: translateY(0);
      opacity: 1;
    }
  }

  @keyframes typing {
    from {
      width: 0;
    }
    to {
      width: 100%;
    }
  }

  @keyframes blink-caret {
    from,
    to {
      border-color: transparent;
    }
    50% {
      border-color: rgb(var(--accent));
    }
  }

  @keyframes floatUp {
    0%,
    100% {
      transform: translateY(0) translateX(-50%);
    }
    50% {
      transform: translateY(-15px) translateX(-50%);
    }
  }

  @keyframes floatRight {
    0%,
    100% {
      transform: translateX(0);
    }
    50% {
      transform: translateX(15px);
    }
  }

  @keyframes floatLeft {
    0%,
    100% {
      transform: translateX(0);
    }
    50% {
      transform: translateX(-15px);
    }
  }

  @keyframes floatDown {
    0%,
    100% {
      transform: translateY(0);
    }
    50% {
      transform: translateY(15px);
    }
  }

  .fade-in {
    animation: fadeIn 1s forwards;
  }

  @keyframes fadeIn {
    from {
      opacity: 0;
    }
    to {
      opacity: 1;
    }
  }

  .slide-right {
    transform: translateX(-50px);
    opacity: 0;
  }

  .slide-left {
    transform: translateX(50px);
    opacity: 0;
  }

  .show {
    opacity: 1;
    transform: translateY(0);
  }

  .show .slide-right,
  .show .slide-left {
    transform: translateX(0);
    opacity: 1;
  }

  /* Dark mode adjustments */
  :global(.dark) .about-container {
    color: #e0e0e0;
  }

  :global(.dark) .mission-card,
  :global(.dark) .timeline-content {
    background: rgba(30, 30, 30, 0.7);
    border-color: rgba(var(--accent), 0.3);
  }

  /* Responsive styles */
  @media (max-width: 768px) {
    .title {
      font-size: 3rem;
    }

    .timeline::before {
      left: 31px;
    }

    .timeline-item {
      width: 100%;
      padding-left: 70px;
      padding-right: 25px;
    }

    .timeline-item:nth-child(odd) {
      left: 0;
      text-align: left;
    }

    .timeline-item:nth-child(even) {
      left: 0;
    }

    .timeline-dot {
      left: 21px;
      right: auto;
    }

    .timeline-content {
      transform: translateX(0) !important;
    }

    .mission-cards {
      flex-direction: column;
      align-items: center;
    }
  }

  .testimonials-section {
    margin-bottom: 6rem;
  }

  .testimonial-slider {
    position: relative;
    max-width: 900px;
    margin: 0 auto;
    overflow: hidden;
  }

  .testimonial-container {
    display: flex;
    transition: transform 0.5s ease;
    gap: 20px;
    width: 100%;
  }

  .testimonial-card {
    flex: 0 0 100%;
    padding: 2rem;
    border-radius: 10px;
    background: rgba(255, 255, 255, 0.05);
    backdrop-filter: blur(10px);
    border: 1px solid rgba(var(--accent), 0.2);
    box-shadow: 0 10px 30px rgba(0, 0, 0, 0.1);
    margin: 1rem 0;
    position: relative;
    transform: translateY(20px);
    opacity: 0;
    animation: fadeInUp 0.6s forwards;
    animation-delay: 0.2s;
  }

  .quote-icon {
    color: #39ff14;
    margin-bottom: 1rem;
    display: flex;
  }

  .quote-icon svg {
    width: 32px;
    height: 32px;
  }

  .testimonial-text {
    font-size: 1.1rem;
    line-height: 1.7;
    margin-bottom: 1.5rem;
    font-style: italic;
  }

  .testimonial-author {
    display: flex;
    align-items: center;
  }

  .author-avatar {
    width: 50px;
    height: 50px;
    border-radius: 50%;
    background: linear-gradient(135deg, rgb(var(--accent)), #39ff14);
    color: white;
    display: flex;
    align-items: center;
    justify-content: center;
    font-weight: bold;
    margin-right: 1rem;
  }

  .author-info h4 {
    margin: 0;
    font-weight: 600;
    color: rgb(var(--accent-light));
  }

  .author-info p {
    margin: 0;
    font-size: 0.9rem;
    opacity: 0.8;
  }

  .testimonial-nav {
    display: flex;
    justify-content: center;
    margin-top: 2rem;
    gap: 0.5rem;
  }

  .nav-dot {
    width: 6px;
    height: 6px;
    border-radius: 50%;
    border: none;
    background: rgba(var(--accent), 0.3);
    cursor: pointer;
    transition: all 0.3s ease;
    padding: 0;
  }

  .nav-dot.active {
    background: #39ff14;
    transform: scale(1.05);
  }

  @keyframes fadeInUp {
    from {
      opacity: 0;
      transform: translateY(20px);
    }
    to {
      opacity: 1;
      transform: translateY(0);
    }
  }

  /* Dark mode adjustments */
  :global(.dark) .testimonial-card {
    background: rgba(30, 30, 30, 0.7);
  }

  /* Responsive styles */
  @media (max-width: 768px) {
    .testimonial-card {
      padding: 1.5rem;
    }

    .testimonial-text {
      font-size: 1rem;
    }
  }

  .read-more-button {
    margin-top: 2rem;
    padding: 12px 24px;
    background: linear-gradient(
      90deg,
      rgb(var(--accent)),
      rgb(var(--accent-light))
    );
    color: white;
    border: none;
    border-radius: 30px;
    font-size: 1rem;
    font-weight: 600;
    cursor: pointer;
    transition:
      transform 0.3s ease,
      box-shadow 0.3s ease;
    box-shadow: 0 5px 15px rgba(var(--accent), 0.4);
  }

  .read-more-button:hover {
    transform: translateY(-3px);
    box-shadow: 0 8px 20px rgba(var(--accent), 0.6);
  }
</style>

<script>
  function scrollToSection() {
    const section = document.querySelector('.about-section') // Replace with the section you want to scroll to
    if (section) {
      section.scrollIntoView({ behavior: 'smooth' })
    }
  }

  function initializeAnimations() {
    const sections = document.querySelectorAll('.fade-in-section')

    const observer = new IntersectionObserver(
      (entries) => {
        entries.forEach((entry) => {
          if (entry.isIntersecting) {
            entry.target.classList.add('show')
          }
        })
      },
      {
        threshold: 0.1
      }
    )

    sections.forEach((section) => {
      observer.observe(section)
    })
  }

  function initializeTestimonials() {
    const container = document.querySelector('.testimonial-container')
    const dots = document.querySelectorAll('.nav-dot')
    let currentIndex = 0

    if (!container || !dots.length) return

    // Hide all testimonials except the first one
    const testimonials = document.querySelectorAll('.testimonial-card')
    testimonials.forEach((card, index) => {
      if (index !== 0) {
        card.style.display = 'none'
      }
    })

    // Add click event listeners to dots
    dots.forEach((dot, index) => {
      dot.addEventListener('click', () => {
        currentIndex = index
        updateSlider()
      })
    })

    function updateSlider() {
      // Hide all testimonials
      testimonials.forEach((card) => {
        card.style.display = 'none'
      })

      // Show current testimonial
      testimonials[currentIndex].style.display = 'block'
      testimonials[currentIndex].style.animation = 'fadeInUp 0.6s forwards'

      // Update active dot
      dots.forEach((dot, index) => {
        if (index === currentIndex) {
          dot.classList.add('active')
        } else {
          dot.classList.remove('active')
        }
      })
    }

    // Auto-scroll
    let interval = setInterval(() => {
      currentIndex = (currentIndex + 1) % testimonials.length
      updateSlider()
    }, 5000)

    // Pause auto-scroll when interacting with navigation
    dots.forEach((dot) => {
      dot.addEventListener('mouseenter', () => {
        clearInterval(interval)
      })

      dot.addEventListener('mouseleave', () => {
        interval = setInterval(() => {
          currentIndex = (currentIndex + 1) % testimonials.length
          updateSlider()
        }, 5000)
      })
    })
  }

  // Initialize everything on page load
  function initializePage() {
    initializeAnimations()
    initializeTestimonials()
  }

  // Run on initial page load
  document.addEventListener('DOMContentLoaded', initializePage)

  // Run on Astro page transitions
  document.addEventListener('astro:page-load', initializePage)

  // Cleanup on page exit
  document.addEventListener('astro:before-swap', () => {
    // Disconnect all existing observers
    document.querySelectorAll('.fade-in-section').forEach((section) => {
      section.classList.remove('show')
    })

    // Clear testimonial interval
    const interval = document.querySelector('.testimonial-container')?._interval
    if (interval) clearInterval(interval)
  })
</script>
