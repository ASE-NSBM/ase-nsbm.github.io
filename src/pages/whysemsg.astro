<!doctype html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>Why SE? - Interactive Games</title>
    <style>
      :root {
        --primary-color: #3b82f6;
        --primary-dark: #2563eb;
        --secondary-color: #6366f1;
        --ui-ux-color: #10b981; /* Green */
        --business-color: #f59e0b; /* Orange/Yellow */
        --project-color: #8b5cf6; /* Purple */
        --light-color: #f8fafc;
        --dark-color: #1e293b;
        --background-color: #eef2f7;
      }

      * {
        margin: 0;
        padding: 0;
        box-sizing: border-box;
      }

      body {
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        background-color: var(--background-color);
        color: var(--dark-color);
        line-height: 1.6;
      }

      .container {
        max-width: 1200px;
        margin: 0 auto;
        padding: 2rem 1rem;
      }

      header {
        text-align: center;
        margin-bottom: 3rem;
        padding: 2rem 0;
        background-color: white;
        border-radius: 15px;
        box-shadow: 0 4px 15px rgba(0, 0, 0, 0.05);
      }

      h1 {
        font-size: 2.5rem;
        margin-bottom: 1rem;
        color: var(--primary-dark);
      }

      .subtitle {
        font-size: 1.25rem;
        color: var(--secondary-color);
        margin-bottom: 1rem;
      }

      .intro {
        max-width: 800px;
        margin: 0 auto 1.5rem;
        font-size: 1.1rem;
      }

      .games-container {
        display: grid;
        grid-template-columns: repeat(auto-fit, minmax(350px, 1fr));
        gap: 25px;
        margin-top: 2rem;
      }

      .game-card {
        background: white;
        border-radius: 15px;
        overflow: hidden;
        box-shadow: 0 8px 20px rgba(0, 0, 0, 0.1);
        transition:
          transform 0.3s ease,
          box-shadow 0.3s ease;
        position: relative;
        display: flex;
        flex-direction: column;
      }

      .game-card:hover {
        transform: translateY(-10px);
        box-shadow: 0 15px 30px rgba(0, 0, 0, 0.15);
      }

      .card-header {
        padding: 1.5rem;
        color: white;
      }

      .ui-ux .card-header {
        background-color: var(--ui-ux-color);
      }

      .business .card-header {
        background-color: var(--business-color);
      }

      .project .card-header {
        background-color: var(--project-color);
      }

      .card-title {
        font-size: 1.5rem;
        margin-bottom: 0.5rem;
      }

      .card-subtitle {
        font-size: 1rem;
        opacity: 0.9;
      }

      .card-body {
        padding: 1.5rem;
        flex-grow: 1;
      }

      .card-description {
        margin-bottom: 1.5rem;
      }

      .play-button {
        display: inline-block;
        background-color: var(--primary-color);
        color: white;
        padding: 0.8rem 2rem;
        border-radius: 50px;
        text-decoration: none;
        font-weight: 600;
        text-align: center;
        transition: all 0.3s ease;
        border: none;
        cursor: pointer;
        font-size: 1rem;
        margin-top: auto;
        box-shadow: 0 4px 10px rgba(0, 0, 0, 0.1);
      }

      .ui-ux .play-button:hover {
        background-color: var(--ui-ux-color);
      }

      .business .play-button:hover {
        background-color: var(--business-color);
      }

      .project .play-button:hover {
        background-color: var(--project-color);
      }

      .coming-soon {
        background-color: var(--secondary-color);
        opacity: 0.9;
        cursor: not-allowed;
      }

      .coming-soon:hover {
        background-color: var(--secondary-color) !important;
        opacity: 1;
      }

      .badge {
        position: absolute;
        top: 15px;
        right: 15px;
        background-color: rgba(255, 255, 255, 0.9);
        color: var(--dark-color);
        padding: 0.25rem 0.75rem;
        border-radius: 20px;
        font-size: 0.75rem;
        font-weight: 600;
        box-shadow: 0 2px 5px rgba(0, 0, 0, 0.1);
      }

      .game-icon {
        font-size: 2.5rem;
        margin-bottom: 1rem;
      }

      .divider {
        height: 1px;
        background-color: #e5e7eb;
        margin: 1.5rem 0;
      }

      footer {
        text-align: center;
        margin-top: 3rem;
        padding: 2rem 0;
        color: var(--secondary-color);
        font-size: 0.9rem;
      }

      /* Responsive adjustments */
      @media (max-width: 768px) {
        .games-container {
          grid-template-columns: repeat(auto-fit, minmax(300px, 1fr));
        }
      }

      @media (max-width: 480px) {
        h1 {
          font-size: 2rem;
        }
        .game-card {
          margin-bottom: 1.5rem;
        }
      }

      /* Animation for cards */
      @keyframes fadeIn {
        from {
          opacity: 0;
          transform: translateY(20px);
        }
        to {
          opacity: 1;
          transform: translateY(0);
        }
      }

      .game-card {
        animation: fadeIn 0.6s ease-out forwards;
        opacity: 0;
      }

      .game-card:nth-child(1) {
        animation-delay: 0.2s;
      }
      .game-card:nth-child(2) {
        animation-delay: 0.4s;
      }
      .game-card:nth-child(3) {
        animation-delay: 0.6s;
      }
    </style>
  </head>
  <body>
    <div class="container">
      <header>
        <h1>Why Software Engineering?</h1>
        <div class="subtitle">Interactive Learning Games</div>
        <p class="intro">
          Test your knowledge and have fun with these interactive Kahoot games
          designed for each session of our boot camp.
        </p>
      </header>

      <div class="games-container">
        <!-- UI/UX Design Game Card -->
        <div class="game-card ui-ux">
          <div class="card-header">
            <h2 class="card-title">UI/UX Design</h2>
            <p class="card-subtitle">Dream up the perfect solution design</p>
          </div>
          <span class="badge">Available Now</span>
          <div class="card-body">
            <p class="card-description">
              Test your understanding of user interface design principles, user
              experience concepts, and design thinking. Can you create
              interfaces that users will love?
            </p>
            <div class="divider"></div>
            <div style="text-align: center;">
              <a
                href="https://play.kahoot.it/v2/?quizId=bdda0d46-a20b-49c9-b5a2-3c9ddef460dd&hostId=92623791-9a8e-4884-892b-0c5ac6fcc0fa"
                class="play-button"
                target="_blank">Play Now</a
              >
            </div>
          </div>
        </div>

        <!-- Business Analysis Game Card -->
        <div class="game-card business">
          <div class="card-header">
            <h2 class="card-title">Business Analysis</h2>
            <p class="card-subtitle">
              Turn business ideas into killer software solutions
            </p>
          </div>
          <span class="badge">Coming Soon</span>
          <div class="card-body">
            <p class="card-description">
              Challenge your analytical thinking and problem-solving skills.
              Learn how to identify business needs and translate them into
              effective software requirements.
            </p>
            <div class="divider"></div>
            <div style="text-align: center;">
              <button class="play-button coming-soon">Coming Soon</button>
            </div>
          </div>
        </div>

        <!-- Project Management Game Card -->
        <div class="game-card project">
          <div class="card-header">
            <h2 class="card-title">Project Management</h2>
            <p class="card-subtitle">Nail down your project plan</p>
          </div>
          <span class="badge">Coming Soon</span>
          <div class="card-body">
            <p class="card-description">
              Put your project management skills to the test! Learn about Agile
              methodologies, task prioritization, and how to lead a software
              project to success.
            </p>
            <div class="divider"></div>
            <div style="text-align: center;">
              <button class="play-button coming-soon">Coming Soon</button>
            </div>
          </div>
        </div>
      </div>

      <footer>
        <p>Part of the "Why Software Engineering?" Boot Camp | ASE NSBM</p>
      </footer>
    </div>

    <script>
      // JavaScript to replace "Coming Soon" with actual links when available
      function updateGameLink(gameId, kahootLink) {
        const gameButton = document.getElementById(gameId)
        if (gameButton && kahootLink) {
          gameButton.href = kahootLink
          gameButton.classList.remove('coming-soon')
          gameButton.textContent = 'Play Now'

          // Update badge
          const card = gameButton.closest('.game-card')
          const badge = card.querySelector('.badge')
          if (badge) {
            badge.textContent = 'Available Now'
          }
        }
      }

      // Function to be called when new links are available
      // Example usage:
      // updateGameLink('business-game', 'https://play.kahoot.it/v2/?quizId=xxxx');
    </script>
  </body>
</html>
